# Lottie assets

Drop the two provided `.lottie` files here with these exact names:

- `bug-hunting.lottie` — copy of `Bug_Hunting.lottie` (magnifier + bug).
  Used in the **How It Works** section, step 4 ("Hunt").
  Recolor its coral fills (`#e87f9b`, `#fc8eaa`) to accent blue `#1C82E0` +
  navy `#21426E` so it matches the cream/navy palette.

- `developer-review.lottie` — copy of `new_ting.lottie` (person at a desk).
  Used in the **For Marshals** section. Its existing `#0070bf` blue is already
  close to `--accent`; only nudge the orange/coral accents toward chart-3/chart-4.

Until these files exist, `LottieOrFallback` renders a static lucide-icon
fallback so the sections stay visually complete — no broken/empty boxes.
